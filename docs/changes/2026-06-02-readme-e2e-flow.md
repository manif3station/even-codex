# 2026-06-02 README End-To-End Flow

## Summary

The `even-codex` release docs now include a dedicated end-to-end walkthrough in
`README.md` for the complete `D2-Codex` operator journey.

## What changed

- added a governed `End-to-End Flow` section to `README.md`
- documented the sequence from bridge start and workspace pairing through phone
  plugin bootstrap, glasses transcript updates, bottom-popup interaction, and
  prompt submission back into Codex
- bound the README flow to repo tests so the release explanation stays aligned
  with the implemented hybrid glasses voice-query slice
