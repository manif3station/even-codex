# 2026-05-31 Protocol Foundation

This change starts implementation work from the shipped specification.

It adds:

- an implementation-mapped `SOW.md`
- the first `Even::Codex::Protocol` runtime contract
- test coverage for event types, required fields, supported commands, and deployment modes
