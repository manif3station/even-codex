# DD-364

## Title

Add the DD-served Even plugin connector surface.

## Method

TDD

## Acceptance

- `even-codex` ships a skill-local `config/routes.json` that exposes a
  governed DD plugin route plus DD ajax connector endpoints
- the DD-served plugin page and assets reuse the paired Codex session data
  through skill-local `/ajax` handlers
- the DD ajax prompt path can submit a new query into the paired Codex session
- the documented operator flow uses the native DD smart routes
  `/app/even-codex/...` and `/ajax/even-codex/...` together with helper-user
  login over HTTPS
- the shared connector logic is covered in Docker tests
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

done
