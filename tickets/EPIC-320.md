# EPIC-320

## Title

Align `even-codex` with Even Hub packaging and SDK requirements.

## Goal

Turn the bundled `even-codex` plugin prototype into a real Even Hub app package for `D2-Codex` that follows the current Even manifest, SDK, UI, and packaging requirements.

## Status

Done
