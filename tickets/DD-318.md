# DD-318

## Title

Add workspace-to-Codex pairing and connector startup to `even-codex`.

## Method

TDD

## Acceptance

- `dashboard even-codex.start add <codex-session-id>` stores the active workspace pairing for the current `WORKSPACE_REF` or `TICKET_REF`
- `dashboard even-codex.start` resolves the paired Codex session for the active workspace and starts a LAN listener on `0.0.0.0:6789` by default
- the connector serves machine-readable health and bootstrap payloads that expose the workspace ref, Codex session id, and plugin entry URL
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
