# DD-362

## Title

Remove the forced transcript rewrite from the first glasses `Up` gesture.

## Method

TDD

## Acceptance

- the first transcript `Up` gesture disables live-follow without forcing the
  transcript content to jump to the top of a review buffer
- the glasses keep the native Even one-line upward movement on that gesture
- while live-follow is frozen, background refresh does not steal the current
  glasses position
- `Down` still resumes live-follow when the operator returns to the bottom
- visible simulator E2E proof outside the Perl `.t` suite shows `Up` no longer
  jumps to the top and `Down` still resumes correctly
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

done
