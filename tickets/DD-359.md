# DD-359

## Title

Fix and prove plugin auto-refresh against live session changes.

## Method

`TDD`

## Acceptance

- the phone-side plugin updates `Latest Prompt`, `Latest Progress`, and
  `Latest Reply` automatically from live bridge session state without a manual
  refresh click
- the status merge treats the server payload as authoritative so stale prompt,
  progress, or reply text does not linger after the live session changes
- Playwright or browser-level tests prove the automatic update behavior
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo
