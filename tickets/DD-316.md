# DD-316

## Title

Add local-network deployment mode to the `even-codex` specification.

## Method

BDD

## Acceptance

- `SPEC.md` explicitly documents a supported LAN-only deployment mode
- the specification distinguishes LAN-reachable hosts from laptop-local `localhost`
- the specification records production constraints for whitelist, CORS, and HTTPS in packaged deployments
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
