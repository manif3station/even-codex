# DD-323

## Title

Improve the `D2-Codex` glasses interface for usability.

## Method

TDD

## Acceptance

- the glasses UI uses multiple readable containers instead of one dense block
- the glasses UI supports simple on-glasses interaction using Even text events
- the glasses UI makes refresh, navigation, and exit behavior legible to the user
- the source uses SDK page rebuild or targeted updates to keep the glasses view synchronized with the phone-side state
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
