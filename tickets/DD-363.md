# DD-363

## Title

Add the `dashboard even-codex.compile` package wrapper.

## Method

TDD

## Acceptance

- `dashboard even-codex.compile` runs the existing Even Hub packaging flow
  without requiring a global `evenhub` binary on `PATH`
- the compile wrapper reuses the Node dependencies that `dashboard skills
  install` already merged into `$HOME/node_modules` when they are available
- the compile wrapper installs local Node dependencies automatically only when
  the installed skill copy still lacks the required packaging binaries
- the compile wrapper reports the selected Hub origin and the generated `.ehpk`
  artifact path
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

done
