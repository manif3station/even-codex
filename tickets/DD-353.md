# DD-353

## Title

Rebuild the glasses layout as transcript plus bottom popup overlay.

## Method

BDD

## Acceptance

- a glasses `Click` opens a bottom popup rectangle instead of replacing the full transcript
- the Codex transcript remains visible above the popup while the popup is open
- the popup disappears after `Send` or `Cancel`
- the visible simulator buttons prove the overlay behavior by screenshot review outside the Perl `.t` suite
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done

## Status

In Progress
