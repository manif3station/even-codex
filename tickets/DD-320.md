# DD-320

## Title

Add a real Even Hub app package for `D2-Codex`.

## Method

TDD

## Acceptance

- the repository includes a real Even Hub `app.json` for `D2-Codex`
- the manifest uses the current documented schema and version floors
- the packaged app entrypoint is a real built file in `dist/`
- the app name, package id shape, versioning, permissions, whitelist, and supported languages match the current docs
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
