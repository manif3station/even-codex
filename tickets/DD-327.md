# DD-327

## Title

Add `dashboard even-codex.e2e {start,stop}`.

## Method

TDD

## Acceptance

- the skill ships a bash CLI entrypoint for `dashboard even-codex.e2e start`
- the skill ships a bash CLI entrypoint for `dashboard even-codex.e2e stop`
- start launches the DD bridge, the local Hub app server, and the simulator as one orchestrated flow
- start records runtime pid and log state for the bridge and app server
- start points the simulator at the served Hub app URL
- stop terminates the tracked bridge and app server processes and delegates simulator shutdown
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
