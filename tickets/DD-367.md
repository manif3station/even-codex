# DD-367

## Title

Prove the HTTPS DD connector modes in the simulator release gate.

## Method

ATDD

## Acceptance

- the simulator runtime uses the real DD HTTPS page on the container's
  non-loopback address and relies on the current DD SAN-aware certificate flow
  without skill-local SAN workarounds
- the release gate proves the helper-login path on the actual noVNC desktop:
  - unauthenticated load shows the DD helper login page
  - helper login reaches the connected `D2-Codex` plugin page
  - the glasses surface shows live transcript content beside that authenticated
    DD page
- the release gate also proves the machine-auth path in the simulator against
  the same DD connector:
  - the DD connector itself can load bootstrap and session data by API key and
    secret over HTTPS
  - the DD connector itself can submit one prompt through
    `/ajax/even-codex/prompt`
  - the packaged Even Hub client stays on the same DD HTTPS page, resolves DD
    smart-route payloads correctly, and reaches `CONNECTED` state in API-key
    mode after helper login
- the simulator regression proof uses screenshot review outside the Perl `.t`
  suite and is recorded in `tickets/TESTING.md`
- the automated Docker gate still passes with `100%` selected coverage
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

complete
