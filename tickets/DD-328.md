# DD-328

## Title

Add multi-connector and multi-session management to the `D2-Codex` Even plugin.

## Method

TDD

## Acceptance

- the phone-side Even plugin stores and manages multiple local DD connector profiles
- the phone-side Even plugin shows a saved list of Codex sessions for the active connector
- the phone-side Even plugin lets the user switch the active connector and the active Codex session
- bridge refresh merges the current bootstrap session into the active connector session list
- the glasses UI can switch between saved sessions for the active connector
- the glasses UI does not switch connectors
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
