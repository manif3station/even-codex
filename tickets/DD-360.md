# DD-360

## Title

Fix and prove the popup voice fallback path.

## Method

`TDD`

## Status

done

## Acceptance

- when browser speech recognition is unavailable, a glasses click does not
  surface a `Voice UNSUPPORTED` dead-end
- the popup guides the user into a usable phone-composer fallback path for
  voice or typing
- the companion textarea is focused automatically for that fallback path
- browser-level or simulator-adjacent tests prove the fallback state and close
  behavior
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo
