# DD-322

## Title

Improve the phone-side `D2-Codex` Even plugin interface.

## Method

TDD

## Acceptance

- the phone-side Hub plugin shows a clear setup and connection workflow instead of a bare origin form
- the plugin exposes refresh and reset actions for bridge setup
- the plugin shows connection, workspace, session, and endpoint summaries in a more readable layout
- the plugin explains the pairing flow and port `6789` usage in-user context
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
