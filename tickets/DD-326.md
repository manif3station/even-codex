# DD-326

## Title

Add `dashboard even-codex.simulator {start,stop}`.

## Method

TDD

## Acceptance

- the skill ships a bash CLI entrypoint for `dashboard even-codex.simulator start`
- the skill ships a bash CLI entrypoint for `dashboard even-codex.simulator stop`
- start records pid and log state under the skill runtime area
- stop terminates the tracked simulator process and cleans the pid file
- the CLI supports override env vars for simulator bin, app URL, automation port, runtime root, and log path
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
