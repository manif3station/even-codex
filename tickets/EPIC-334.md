# EPIC-334

## Title

Add query input control to the `D2-Codex` simulator flow.

## Goal

Let the Even plugin and glasses flow stage a new Codex query in a user-friendly way that can be proven inside the Dockerized simulator, and close the release only when the live container shows prompt and reply propagation end to end.

## Tickets

- `DD-334` Add a staged query composer and glasses focus controls to `D2-Codex`.
- `DD-335` Prove the live simulator input flow with screenshot-reviewed E2E checks.

## Status

Done
