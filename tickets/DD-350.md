# DD-350

## Title

Align the glasses click, swipe, and double-click interaction flow in `D2-Codex`.

## Method

BDD

## Acceptance

- the glasses default surface remains a live Codex transcript window
- a glasses click opens the staged query input view instead of refreshing or changing layout silently
- glasses swipe up and swipe down only cycle the staged action when the input view is open
- a glasses double-click closes the input view and returns to the transcript surface
- the simulator E2E proof includes fresh screenshot review outside the Perl `.t` suite for click, swipe, and double-click reactions
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
