# DD-357

## Title

Launch the packaged native Codex binary in the simulator xterm.

## Method

`TDD`

## Acceptance

- the simulator Docker image publishes the packaged native Codex binary path
  instead of the Node launcher wrapper path
- the simulator launcher scripts use that packaged native binary path by
  default
- repository tests assert the simulator source stays on the packaged native
  binary route
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo
