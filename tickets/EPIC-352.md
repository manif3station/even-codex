# EPIC-352

## Title

Keep the glasses transcript visible when no staged query exists.

## Goal

Prevent a manual glasses click from replacing the transcript with a useless
`No staged query` input view. The glasses click path should only open the staged
input surface when a real draft exists.

## Tickets

- `DD-352` Guard the glasses click path when no staged query exists.

## Status

Done
