# DD-358

## Title

Record and guard the exact simulator launcher E2E gate.

## Method

`BDD`

## Acceptance

- the `even-codex` release rules explicitly require a fresh
  `dashboard even-codex.simulator start` proof for simulator-launcher or
  Codex-startup tickets
- the governed docs record that the installed skill copy must be current before
  that launcher proof is trusted
- repository tests assert the release documentation keeps the exact
  simulator-launcher gate language
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo
