# DD-334

## Title

Add a staged query composer and glasses focus controls to `D2-Codex`.

## Method

TDD

## Acceptance

- the phone-side Even plugin supports composing a staged query, slash normalization, and explicit `Send`, `Retry`, and `Cancel` actions
- the glasses UI supports up or down navigation semantics, click-to-focus, and double-click-to-restore within the current SDK event model
- the plugin and glasses views clearly reflect staged input state and latest sent query state
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
