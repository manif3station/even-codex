# DD-329

## Title

Add Dockerized noVNC simulator orchestration to `dashboard even-codex.simulator`.

## Method

TDD

## Acceptance

- the skill ships a Dockerfile in the skill repo for a simulator desktop image based on `developer-dashboard:latest`
- the skill ships a skill-local Docker Compose file for the simulator desktop
- `dashboard even-codex.simulator start` launches the Dockerized noVNC environment and exposes the host viewing port on `15700` by default
- the start flow resolves the active workspace pairing and injects it into the containerized `even-codex` environment
- the container starts the DD environment, the `even-codex` bridge chain, and the Even simulator without additional manual setup
- `dashboard even-codex.simulator stop` tears the environment down cleanly
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
