# DD-325

## Title

Add a simulator-backed screenshot workflow for the `D2-Codex` Hub listing.

## Method

TDD

## Acceptance

- the repo includes a repeatable screenshot-capture workflow for `D2-Codex`
- the workflow targets the Even Hub simulator control plane as documented
- the repository stores submission-ready screenshot outputs or the governed capture location for them
- documentation explains how to regenerate the screenshots from the current app build
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
