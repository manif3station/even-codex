# DD-352

## Title

Guard the glasses click path when no staged query exists.

## Method

BDD

## Acceptance

- a glasses `Click` with no staged query keeps the transcript visible
- the glasses input view only opens when there is a staged query, draft query, or reusable submitted query
- the phone-side state records a useful status message when the user clicks glasses input with nothing staged
- fresh screenshot review outside the Perl `.t` suite proves the no-staged-query click case stays on transcript
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
