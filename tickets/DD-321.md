# DD-321

## Title

Rebuild the `D2-Codex` frontend around the Even Hub SDK and packaging flow.

## Method

TDD

## Acceptance

- the app uses `@evenrealities/even_hub_sdk`
- the frontend implements a first-run message instead of a blank screen
- the frontend uses an Even-compatible greyscale / no-background-fill layout
- the packaged app exposes a root double-tap exit path through `bridge.shutDownPageContainer(1)`
- the repo can build the app and package it with `evenhub pack`
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
