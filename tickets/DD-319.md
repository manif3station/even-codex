# DD-319

## Title

Bundle the first Even plugin web app inside `even-codex`.

## Method

TDD

## Acceptance

- the skill repository includes a bundled Even plugin web app alongside the DD connector runtime
- the plugin loads connector bootstrap data from the local LAN bridge and renders the paired workspace ref, Codex session id, host, and port
- the plugin bundle is reachable from the same bridge started by `dashboard even-codex.start`
- browser-facing validation uses Playwright when the toolchain is available
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
