# DD-332

## Title

Add host-UID user creation and non-root simulator runtime.

## Method

TDD

## Acceptance

- the simulator Docker image creates a dedicated non-root user for the desktop runtime
- `dashboard even-codex.simulator start` passes the host caller UID and GID into the compose stack
- the container runtime switches to that created user before touching the mounted host `~/.codex` tree
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
