# DD-330

## Title

Install Codex in the simulator Docker image and wire auth reuse.

## Method

TDD

## Acceptance

- the simulator Dockerfile installs the `codex` CLI inside the container
- the simulator compose stack mounts the host Codex config into the container so existing auth can be reused
- the container startup flow records that Codex is present in the simulator desktop environment
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
