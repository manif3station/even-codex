# EPIC-355

## Title

Add a hybrid voice query composer to `D2-Codex`.

## Goal

Let a glasses click open a real voice-driven query flow that captures speech in
the Even Hub webview layer, mirrors the recognised text into the glasses popup,
and submits through the existing staged-query path.

## Ticket

- `DD-355` Implement and prove the hybrid glasses voice-query flow in
  `D2-Codex`.
