# EPIC-329

## Title

Add a noVNC Docker simulator environment for `D2-Codex`.

## Goal

Make `dashboard even-codex.simulator start` launch a self-contained Docker desktop based on `developer-dashboard:latest` with the skill, bridge, Hub app, and Even simulator already running behind noVNC on port `15700`.

## Status

Done
