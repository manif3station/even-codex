# DD-331

## Title

Add transcript routing, Codex TUI launch, and screenshot-backed E2E proof.

## Method

TDD

## Acceptance

- the simulator desktop launches a visible Codex TUI for the paired session inside the VNC environment
- the `even-codex` bridge exposes the latest paired Codex prompt and reply from the mounted Codex session store
- the Even plugin and glasses UI render that latest prompt and reply clearly
- the release gate captures screenshots that prove the Codex window, webview, and glasses surfaces reflect the paired conversation
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
