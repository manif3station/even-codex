# EPIC-318

## Title

Ship the first runnable `even-codex` LAN bridge.

## Goal

Deliver the first real `even-codex` runtime slice as a local DD connector and bundled Even plugin that pair a workspace to a Codex session and expose a LAN bridge for the phone-hosted Even app.

## Status

Done
