# DD-335

## Title

Prove the live simulator input flow with screenshot-reviewed E2E checks.

## Method

BDD

## Acceptance

- the live noVNC container proves `hi -> Hi` in the Codex TUI
- the live Even simulator proves the same `hi -> Hi` transcript on plugin and glasses surfaces
- the live Even plugin proves the new input workflow state through fresh screenshot review outside the Perl `.t` suite
- screenshot interpretation stays outside the Perl `.t` suite and is documented as an LLM-reviewed release check
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
