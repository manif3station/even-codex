# EPIC-327

## Title

Add one-command desktop E2E flow for `D2-Codex`.

## Goal

Provide a single bash CLI that starts and stops the local bridge, Hub app server, and Even simulator together so the full desktop `codex -> dd connector -> Even Plugin -> simulator` path is easy to launch.

## Status

Done
