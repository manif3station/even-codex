# DD-315

## Title

Write the `even-codex` production software specification.

## Method

BDD

## Acceptance

- `SPEC.md` defines the production architecture, interfaces, constraints, security model, operational requirements, phased delivery plan, and testing expectations
- `skills/even-codex/` exists as an isolated governed skill repository with `README.md`, `docs/`, `tickets/`, `.env`, `Changes`, and `LICENSE`
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
