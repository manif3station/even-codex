# DD-361

## Title

Add transcript bottom-stick state and preserve manual scroll review.

## Method

TDD

## Acceptance

- the glasses transcript starts in live-follow mode at the newest bottom line
- transcript refreshes do not bounce the user back to the top
- after the user scrolls up to inspect older transcript lines, auto-refresh does not steal focus or move the transcript unexpectedly
- after the user returns to the newest bottom line, live-follow mode resumes
- visible simulator E2E proof outside the Perl `.t` suite demonstrates the reported bug is fixed
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

done

## Result

- shipped in `0.31`
- proven on the live simulator with long wrapped transcript content:
  - fresh start showed the newest reply tail with `END-MARKER`
  - `Up` froze the glasses while the phone-side plugin advanced to `NEXT-END`
  - `Down` resumed live-follow and the glasses caught up to `NEXT-END`
