# DD-351

## Title

Fix and prove the manual simulator glasses button path in `D2-Codex`.

## Method

BDD

## Acceptance

- the visible simulator `Click` button opens the glasses staged input view
- the visible simulator `Up` or `Down` buttons change the staged action while that input view is open
- the visible simulator `Double click` button returns the glasses to the transcript view
- a staged prompt can be sent from the glasses path using the visible simulator buttons and the resulting prompt or reply progression is visible on Codex, the phone plugin, and the glasses transcript
- fresh screenshot review outside the Perl `.t` suite proves the manual button path and the glasses-sent prompt path
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
