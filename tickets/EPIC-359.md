# EPIC-359

## Title

Prove plugin auto-refresh tracks live session status.

## Goal

Make the phone-side `D2-Codex` plugin reflect the real paired session status
automatically and prove that the panels update without manual refresh clicks.

## Ticket

- `DD-359` Fix and prove plugin auto-refresh against live session changes.
