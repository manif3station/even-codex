# EPIC-330

## Title

Add Codex CLI to the Dockerized simulator desktop.

## Goal

Make the noVNC simulator image include the `codex` CLI itself so the full E2E container has Developer Dashboard, `even-codex`, Even Hub simulator, and Codex available together.

## Status

Done
