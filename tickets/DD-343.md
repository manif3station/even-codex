# DD-343

## Title

Add real staged-query submission and glasses conversation proof to `D2-Codex`.

## Method

TDD

## Acceptance

- the bridge accepts a staged query submission and forwards it into the paired Codex session
- the live simulator proves `what is the year today?` and `what is the month of today?` as real Codex turns
- the Codex TUI, Even plugin, and glasses surfaces all show the resulting prompt and reply progression
- the glasses view stays on a single scrolling transcript surface and does not swap into draft or action panes during swipe input
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
