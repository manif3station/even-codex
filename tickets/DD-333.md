# DD-333

## Title

Add an LLM-reviewed screenshot acceptance gate for the simulator E2E flow.

## Method

BDD

## Acceptance

- the release gate captures fresh noVNC desktop screenshots from the live simulator container
- screenshot interpretation stays outside the Perl `.t` suite and is documented as a human or LLM-reviewed release check
- the documented acceptance check proves both the Codex TUI `hi -> response` turn and the matching Even plugin or glasses transcript view
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `even-codex` repo

## Status

Done
